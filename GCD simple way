# simpler way but with less time complexity
def gcd(m, n):
    cf = []
    for i in range(1, min(m,n)+1):
        if (m%i) == 0 and (n%i) == 0:
            cf.append(i)
    print("Greatest common divisor of", m, "and", n, "is:", cf[-1])
    return
m = int(input())
n = int(input())
print(gcd(m,n))
